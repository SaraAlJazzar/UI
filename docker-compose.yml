services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port 8000

    env_file:
      - .env

    network_mode: host

    volumes:
      - .:/app

    restart: unless-stopped
